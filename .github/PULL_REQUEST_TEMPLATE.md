## What does this PR do?

Brief description of the changes introduced by this pull request.

Closes #

## Changes

- 
- 

## How was it tested?

- [ ] Automated tests (Backend: `go test -v -race ./...`, Frontend: `npm test`)
- [ ] Static analysis & linting (`go vet ./...`, `npm run lint`, `npm run typecheck`)
- [ ] Manual end-to-end verification (e.g. verified in browser / curl)

## Checklist

- [ ] All automated tests pass cleanly
- [ ] New behavior is tested where appropriate
- [ ] OpenAPI specification (`docs/openapi/openapi.yaml`) updated if API routes/schemas changed
- [ ] Database migrations (`api/db/migrations/`) included if schema changed
- [ ] Documentation updated if architecture or setup changed
- [ ] No real personal data, credentials, or `.env` files committed
